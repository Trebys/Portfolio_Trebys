<a
  class="social-pill"
  {...Astro.props}
  target="_blank"
  rel="noopener noreferrer"
>
  <slot />
</a>
<style>
  .social-pill {
    --color: #00A97F;
    --color2: rgb(10, 25, 30);
    padding: 0.8em 1.75em;
    background-color: black; /* Fondo negro por defecto */
    border-radius: 6px;
    border: 0.3px solid var(--color);
    transition: 0.5s;
    position: relative;
    overflow: hidden;
    cursor: pointer;
    z-index: 1;
    font-weight: 300;
    font-size: 17px;
    font-family: 'Roboto', 'Segoe UI', sans-serif;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5em;
    color: white; /* Color blanco del texto por defecto */
  }

  .social-pill::after, .social-pill::before {
    content: '';
    display: block;
    height: 100%;
    width: 100%;
    transform: skew(90deg) translate(-50%, -50%);
    position: absolute;
    inset: 50%;
    left: 25%;
    z-index: -1;
    transition: 0.5s ease-out;
    background-color: var(--color);
  }

  .social-pill::before {
    top: -50%;
    left: -25%;
    transform: skew(90deg) rotate(180deg) translate(-50%, -50%);
  }

  .social-pill:hover::before {
    transform: skew(45deg) rotate(180deg) translate(-50%, -50%);
  }

  .social-pill:hover::after {
    transform: skew(45deg) translate(-50%, -50%);
  }

  .social-pill:hover {
    color: var(--color2); /* Color del texto cuando se hace hover */
  }

  .social-pill:active {
    filter: brightness(0.7);
    transform: scale(0.98);
  }

  /* Modo claro */
  @media (prefers-color-scheme: light) {
    .social-pill {
      color: white; /* Texto e icono en color negro */
    }

    .social-pill:hover {
      color: white; /* Texto e icono en color blanco al hacer hover */
    }
  }

  @media (min-width: 640px) {
    .social-pill {
      padding: 0.6em 1.2em;
      font-size: 1.1rem;
    }
  }

  @media (min-width: 768px) {
    .social-pill {
      padding: 0.7em 1.4em;
      font-size: 1.2rem;
    }
  }

  @media (min-width: 1024px) {
    .social-pill {
      padding: 0.8em 1.75em;
      font-size: 1.25rem;
    }
  }
</style>

